<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mtkresearch.breezeapp.engine">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Signature-level permission for BreezeAppEngineService -->
    <permission
        android:name="com.mtkresearch.breezeapp.permission.BIND_AI_ROUTER_SERVICE"
        android:protectionLevel="signature" />

    <application
        android:allowBackup="true"
        android:label="BreezeApp Engine"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        <!-- Neuron SDK Libraries for MTK NPU Support -->
        <uses-native-library
            android:name="libapuwareutils_v2.mtk.so"
            android:required="false" />

        <uses-native-library
            android:name="libapuwareapusys_v2.mtk.so"
            android:required="false" />

        <uses-native-library
            android:name="libnir_neon_driver_ndk.mtk.so"
            android:required="false" />

        <uses-native-library
            android:name="libnir_neon_driver_ndk.mtk.vndk.so"
            android:required="false" />

        <uses-native-library
            android:name="libcmdl_ndk.mtk.vndk.so"
            android:required="false" />

        <uses-native-library
            android:name="libcmdl_ndk.mtk.so"
            android:required="false" />

        <uses-native-library
            android:name="libneuronusdk_adapter.mtk.so"
            android:required="false" />
        
        <uses-native-library
            android:name="libc++_shared.so"
            android:required="false" />


        <!-- Engine Entry Activity - Starts BreezeApp Engine Service -->
        <activity
            android:name=".ui.BreezeAppEngineLauncherActivity"
            android:exported="true"
            android:label="BreezeApp Engine"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- BreezeAppEngineService declaration - Foreground Service for AI Processing -->
        <service
            android:name=".BreezeAppEngineService"
            android:enabled="true"
            android:exported="true"
            android:foregroundServiceType="dataSync"
            android:stopWithTask="false"
            android:permission="com.mtkresearch.breezeapp.permission.BIND_AI_ROUTER_SERVICE">
            <intent-filter>
                <action android:name="com.mtkresearch.breezeapp.engine.BreezeAppEngineService" />
            </intent-filter>
        </service>


    </application>

</manifest> 