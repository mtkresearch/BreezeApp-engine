<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!--
    This is the network security configuration for the BreezeApp Engine library.
    It follows the principle of being SECURE BY DEFAULT for release builds,
    while providing convenience for DEBUG builds.
    -->

    <!-- Base configuration for RELEASE builds -->
    <!-- This policy enforces HTTPS for all connections. No cleartext (http) is permitted. -->
    <!-- If a client app developer needs to allow http or trust custom certificates -->
    <!-- for their production app, they MUST create their own network_security_config.xml -->
    <!-- in their app, which will override this library's secure default. -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- DEBUG-ONLY OVERRIDES -->
    <!-- This block is only used when the app is built with android:debuggable="true". -->
    <!-- It allows cleartext (http) traffic to any host for local development convenience. -->
    <debug-overrides>
        <base-config cleartextTrafficPermitted="true">
            <trust-anchors>
                <certificates src="system" />
            </trust-anchors>
        </base-config>
    </debug-overrides>

</network-security-config>